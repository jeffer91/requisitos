(function(window){
  "use strict";
  window.ExcelLocalBridge = window.ExcelLocalBridge || {
    version:"bdlocal-compat",
    ready:function(){ return true; },
    syncFromBDLocal:function(){ return true; }
  };
})(window);
